package com.example.sns.service.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.*;

@SpringBootTest
class DemoStarterSnsApplicationTests {

	@Test
	void contextLoads() {
		Set<String> vetor = new HashSet<>();
		List<String> vetorLista = new ArrayList<>();
		vetorLista.forEach(valor -> {});
		Map<String, String> matriz = new HashMap<>();
		matriz.forEach((chave, valor) -> {
		});
	}
}
